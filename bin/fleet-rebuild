#!/usr/bin/env bash

# Look for the fleet repo
fleet_repo="$HOME/code/analogrelay/fleet"
if [ ! -d "$fleet_repo" ]; then
  echo "Fleet repo not found at $fleet_repo"
  exit 1
fi

cd $fleet_repo
script/rebuild