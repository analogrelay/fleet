- name: Configure OS settings
  include_tasks: "settings.{{ ansible_distribution | lower }}.yml"

- name: Install base package management
  ansible.builtin.include_tasks: "packages.{{ ansible_distribution | lower }}.yml"
